import pandas as pd

df = pd.read_csv("price.csv")

df.drop(['carat'], axis=1)

df.groupby(['color'], axis=0).sum()

pd.get_dummies(df, prefix=None, dummy_na=False, columns=None, sparse=False, drop_first=False, dtype=bool)

df.replace(to_replace=r"^tru.$", value="True", limit=None, regex=True)

df.replace(to_replace=r"^tru.$", value="True", limit=None, regex=True)
